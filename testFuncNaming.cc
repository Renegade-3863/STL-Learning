#include <iostream>
#include <functional>
#include <algorithm>

/* 用来检查C++是如何命名函数的 */
void* workThread(void* arg)
{
    return nullptr;
}

/* 用来检查C++是如何命名函数的 */
int normalFunc(int arg)
{
    return 0;
}

int main(int argc, char* argv[])
{
    return 0;
}